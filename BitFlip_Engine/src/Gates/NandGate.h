#include "AndGate.h"

// ─────────────────────────────────────────────────────────────────────────────
// AND
// ─────────────────────────────────────────────────────────────────────────────
bool AndGate::evaluate(const std::vector<SignalState>& inputs,
                       std::vector<SignalState>&       outputs)
{
    // TODO — AND all inputs, write result to outputs[0]
    // return true if outputs[0] changed
}

// ─────────────────────────────────────────────────────────────────────────────
// OR
// ─────────────────────────────────────────────────────────────────────────────
bool OrGate::evaluate(const std::vector<SignalState>& inputs,
                      std::vector<SignalState>&       outputs)
{
    // TODO
}

// ─────────────────────────────────────────────────────────────────────────────
// NOT
// ─────────────────────────────────────────────────────────────────────────────
bool NotGate::evaluate(const std::vector<SignalState>& inputs,
                       std::vector<SignalState>&       outputs)
{
    // TODO
}

// ─────────────────────────────────────────────────────────────────────────────
// NAND
// ─────────────────────────────────────────────────────────────────────────────
bool NandGate::evaluate(const std::vector<SignalState>& inputs,
                        std::vector<SignalState>&       outputs)
{
    // TODO
}

// ─────────────────────────────────────────────────────────────────────────────
// NOR
// ─────────────────────────────────────────────────────────────────────────────
bool NorGate::evaluate(const std::vector<SignalState>& inputs,
                       std::vector<SignalState>&       outputs)
{
    // TODO
}

// ─────────────────────────────────────────────────────────────────────────────
// XOR
// ─────────────────────────────────────────────────────────────────────────────
bool XorGate::evaluate(const std::vector<SignalState>& inputs,
                       std::vector<SignalState>&       outputs)
{
    // TODO
}

// ─────────────────────────────────────────────────────────────────────────────
// XNOR
// ─────────────────────────────────────────────────────────────────────────────
bool XnorGate::evaluate(const std::vector<SignalState>& inputs,
                        std::vector<SignalState>&       outputs)
{
    // TODO
}